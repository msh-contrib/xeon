var fs = require('fs');
var grapher = require('./grapher');

function Reader() {
	//pass
}

Reader.loadFile = function (filename) {

};

Reader.createDepsGraphFromFile = function () {
	this._depFile;
}